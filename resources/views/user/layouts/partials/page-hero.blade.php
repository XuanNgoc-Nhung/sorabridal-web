<section class="page-hero">
  <div class="container">
    <div class="page-hero-inner sr d1">
      <span class="sec-tag">{{ $eyebrow }}</span>
      <h1 class="page-hero-title">{{ $title }}</h1>
      @if(!empty($subtitle))
        <p class="page-hero-sub">{{ $subtitle }}</p>
      @endif
    </div>
  </div>
</section>
